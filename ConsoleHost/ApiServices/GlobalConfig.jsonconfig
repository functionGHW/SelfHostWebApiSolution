### 
### 以"#"为第一个非空白字符的行作为注释，解析时忽略。
### 配置信息，JSON格式。
### 
### 
[
  {
    # 路由名称
    "Name": "Default",
    # 路由模版
    "RouteTemplate": "{controller}/{action}",
    # 控制器类型所在命名空间列表
    "Namespaces": [ "DemoService.Controllers" ],
    # 默认路由参数
    "Defaults": {
      "controller": "Test",
      "action": "Index"
    }
  }
]
